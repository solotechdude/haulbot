{
  "name": "@relaybooking/bot",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env.development.local --env-file=../../.env run --watch src/index.ts",
    "start": "bun --env-file=../../.env.development.local --env-file=../../.env run src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@relaybooking/shared": "workspace:*",
    "grammy": "^1.35.0"
  }
}
